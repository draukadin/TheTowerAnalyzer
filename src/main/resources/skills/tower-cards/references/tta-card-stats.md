# Card Stats

Raw per-level stat tables for all 31 cards (values at each star level, 1–7 stars).

> **TODO:** Populate this file with the full per-level value tables for every card.
> Until then, treat card stat questions with the inline guidance in `../SKILL.md` and
> verify exact numbers against the in-game card screen.
